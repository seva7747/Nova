export function Footer() {
  return (
    <footer className="w-full py-10 px-6 text-center text-xs text-white/25">
      Nova is a software prototype for a future hardware product. No wake word is processed on a server — your
      voice is only sent to the backend after "Nova" is heard, for the length of one command.
    </footer>
  );
}
